# Guised Up — embedding service dependencies (top-level).
# For a reproducible production install, generate a lockfile with `pip freeze`.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2
sentence-transformers>=3,<6
psycopg2-binary>=2.9

# Dev / test
pytest>=8
httpx>=0.27
